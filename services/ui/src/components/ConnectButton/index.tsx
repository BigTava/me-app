/*import React from "react";

const ConnectButton = () => {
  return (
    <div
      className="selector-wrapper"
      ref={React.useRef<HTMLDivElement>(null)}
      onMouseEnter={useOpenModal(ApplicationModal.NETWORK_SELECTOR)}
      onMouseLeave={useCloseModal(ApplicationModal.NETWORK_SELECTOR)}
      onClick={isMobile ? toggleModal : undefined}
    ></div>
  );
};

export default ConnectButton;*/
export {};
